spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: EARN_WHILE_TRAVEL
    password: root
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /

server:
  port: 8600
  servlet:
    context-path: /ewt

logging:
  level:
    com.zaxxer.hikari: ERROR  # 🔹 Correct indentation here!
